Howdy xmms2 users,

just trying to compile and install xmms2.
It is easy "scons && scons && scons /usr".

But after second scons I gen only:

----
scons: Reading SConscript files ...
Cachefile scons.cache found, not checking libs
'''garbage in flags: -z'''
----

There is a problem with "curl". It can't be compiled and linked with combreloc option (the same problem is with ecore).
I use the source based linux distro Source Mage GNU/Linux.

After first scons (curl with combreloc):
Module curl: -L/usr/lib -lcurl -z combreloc -s -lssl -lcrypto -ldl -lssl -lcrypto -ldl -lz -DHAVE_CURL

After first scons (curl without combreloc):
Module curl: -L/usr/lib -lcurl -s -lssl -lcrypto -ldl -lssl -lcrypto -ldl -lz -DHAVE_CURL

If I install "curl" without combreloc scons is OK.

Is it a feature of scons?

- ladislav hagara -
- http://www.sourcemage.org -